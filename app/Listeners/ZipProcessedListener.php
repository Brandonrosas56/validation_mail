<?php
/* namespace App\Listeners;

use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Queue\InteractsWithQueue;
 */
/* class ZipProcessedListener
{
    use InteractsWithQueue;

    public function handle($idArchivoZip)
    {
        // Redirigir al controlador MetadataController
        $metadataController = new MetadataController();
        return $metadataController->show(request()->merge(['idArchivoZip' => $idArchivoZip]));
    }
} */